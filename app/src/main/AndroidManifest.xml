<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.atguigu.bilibili">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".activity.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".modle.tuijian.activity.TypeActivity" />
        <activity android:name=".modle.zhibo.activity.BannerWebViewActivity" />
        <activity android:name=".modle.faxian.activity.InterestActivity" />
        <activity android:name=".modle.faxian.activity.TopicActivity" />
        <activity android:name=".modle.faxian.activity.OriginalRankActivity" />
        <activity android:name=".activity.SettingsActivity"></activity>
    </application>

</manifest>